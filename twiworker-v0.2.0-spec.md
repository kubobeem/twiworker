# twiworker v0.2.0 アップデート仕様書

> **作成日**: 2026-07-28  
> **バージョン**: 0.1.0 → 0.2.0  
> **期限**: 1週間以内

---

## 1. 概要

twiworker を **バージョン 0.2.0** にアップデートする。  
以下の3軸でバランスよく（50:50）改善を行う：

| 軸 | 内容 |
|----|------|
| 🎨 **UI/UX刷新** | Twitter/X の現行デザインに寄せたフロントエンド全面リデザイン |
| ⚡ **機能拡充** | twikit 相当の全機能をバックエンド・フロントエンドに追加 |
| 🛡️ **品質向上** | ユニットテスト導入、型定義強化、ドキュメント更新、エラーハンドリング改善 |

---

## 2. アーキテクチャ変更

### 2.1 レイアウト構成（3カラム）

X（旧Twitter）の現行レイアウトに準拠した3カラム構成に変更する：

```
┌──────────────┬──────────────────────┬─────────────────┐
│  左カラム    │     中央カラム        │    右カラム      │
│  (Navigation)│    (Main Content)     │  (Widgets)       │
│              │                      │                 │
│  🐦 ロゴ     │  各ページのメイン     │  🔍 検索        │
│              │  コンテンツ           │                  │
│  📊 ホーム   │  ・タイムライン       │  📈 トレンド     │
│  🔍 探索     │  ・ツイート詳細       │   （全世界/日本） │
│  🔔 通知     │  ・プロフィール       │                  │
│  ✉️ DM      │  ・ etc.              │  ✨ おすすめ     │
│  📋 リスト   │                      │   ユーザー       │
│  👤 プロフ   │                      │                  │
│  📅 予約     │                      │                  │
│  ⚙️ 設定     │                      │                  │
└──────────────┴──────────────────────┴─────────────────┘
```

### 2.2 サイドバー（左カラム）

- アイコン中心のモダンなナビゲーション（X準拠）
- アクティブページの強調表示
- モバイル時はハンバーガーメニューに格納（現状維持でOK）
- ショートカットキー `1`-`8` は拡張して新しいページに対応

### 2.3 右カラム（Widgets）

| Widget | 内容 |
|--------|------|
| 🔍 検索バー | キーワード検索（インクリメンタル） |
| 📈 トレンド | 全世界/日本のトレンド表示（タブ切替） |
| ✨ おすすめユーザー | フォロー候補（将来的に） |

---

## 3. フロントエンド刷新

### 3.1 デザインガイドライン

- **カラーパレット**: X（Twitter）の現行カラーに準拠
  - 背景: `#000000`（ダーク）/ `#ffffff`（ライト）
  - アクセント: `#1d9bf0`（Xブルー）
  - 危険: `#f4212e`
  - 成功: `#00ba7c`
  - テキスト: 白/黒 + グレー階層
- **フォント**: システムフォント（-apple-system, BlinkMacSystemFont, sans-serif）
- **角丸**: 大（16px〜20px）のカード、フルラウンドのボタン
- **グラスモーフィズム**: 現状のグラスモーフィズムから、Xのフラットデザインに移行（またはユーザー設定で切替）

### 3.2 ツイートカード再設計

現在のシンプルなカードから、X風のツイートカードに刷新：

| 要素 | 現状 | 変更後 |
|------|------|--------|
| アバター | イニシャル1文字 | 円形アバター（文字 or 画像URL対応） |
| ユーザー名 | 横一列 | 名前 + @screen_name + 日時の横並び |
| 三点リーダー | なし | ツイート操作メニュー（削除など） |
| アクション | 💬🔄❤️ テキスト付き | 💬🔄❤️🔁（アイコンのみ、カウント表示） |
| 返信フォーム | インライン表示 | インライン + アバター付き |
| インタラクション | クリックのみ | ホバーで色変化、アニメーション |

### 3.3 ページ構成（既存8ページ + 新規ページ）

#### 既存ページのリデザイン

| ページ | 現状 | 変更後（X風） |
|--------|------|--------------|
| 📊 ダッシュボード | 統計カード+最近のツイート | X「ホーム」風に統合。左にTL、右にトレンド |
| ✍️ コンポーズ | 独立ページ、テキストエリア | Xのツイート作成モーダル/ポップアップ風に |
| 🔍 検索 | 独立ページ（フォーム+結果） | 右カラム検索＋専用ページ両方に対応 |
| 📰 タイムライン | ツイート一覧＋無限スクロール | XホームTLと同様の表示（レコメンデーション含む） |
| ✉️ DM | 一覧＋送信フォーム | XのDM画面風（2ペイン: 会話リスト＋メッセージ） |
| 📅 スケジュール | 予約作成＋一覧 | 現状維持＋X風スタイリング |
| 📈 トレンド | タブ切替で表示 | 右カラム常駐＋クリックで詳細ページ |
| ⚙️ 設定 | ステータス＋Cron＋免責 | X風設定画面（カテゴリ分け） |

#### 新規ページ

| ページ | 説明 |
|--------|------|
| 👤 プロフィール | ユーザー情報表示（ツイート/返信/いいね/メディア タブ） |
| 🔔 通知 | 通知一覧（フォロー、いいね、リツイート、返信） |
| 📋 リスト | リスト一覧表示・作成・ツイート表示 |
| 🔖 ブックマーク | ブックマークしたツイート一覧 |

### 3.4 キーボードショートカット拡張

| キー | 操作 | 状態 |
|------|------|------|
| `1`-`9` | ページ切替 | ✅ 拡張（9: プロフィール） |
| `N` | 新規ツイート | ✅ 現状維持 |
| `/` | 検索にフォーカス | ✅ 現状維持 |
| `R` | 更新 | ✅ 現状維持 |
| `L` | いいね一覧表示 | 🆕 追加 |
| `B` | ブックマーク | 🆕 追加 |
| `Esc` | モーダル/ドロップダウンを閉じる | 🆕 追加 |
| `.` | メニューを開く | 🆕 追加（X準拠） |

---

## 4. バックエンド機能拡充

### 4.1 基本方針

- `src/client.ts` の TwitterClient クラスを拡張
- twikit の最新コード（v2.3.1以降）を参照して実装
- 新しいハンドラーは `src/handlers/` に追加
- GraphQL クエリIDは最新の twikit 準拠に更新

### 4.2 追加機能一覧

| 機能 | カテゴリ | エンドポイント | 状態 |
|------|---------|---------------|------|
| **リスト一覧取得** | List | `GET /api/lists` | 🆕 |
| **リスト作成** | List | `POST /api/lists` | 🆕 |
| **リストのツイート取得** | List | `GET /api/lists/:id/tweets` | 🆕 |
| **リストメンバー管理** | List | `GET/POST /api/lists/:id/members` | 🆕 |
| **通知一覧取得** | Notification | `GET /api/notifications` | 🆕 |
| **通知フィルター** | Notification | `GET /api/notifications?filter=` | 🆕 |
| **スペース情報取得** | Space | `GET /api/spaces/:id` | 🆕 |
| **スペース検索** | Space | `GET /api/spaces/search?q=` | 🆕 |
| **投票付きツイート投稿** | Poll | `POST /api/tweet` 拡張（pollオプション追加） | 🆕 |
| **投票に投票** | Poll | `POST /api/tweet/:id/vote` | 🆕 |
| **センシティブ設定** | Settings | `GET/PUT /api/settings/content` | 🆕 |
| **DM会話一覧** | DM強化 | `GET /api/dm/conversations` | 🆕 |
| **DMスレッド表示** | DM強化 | `GET /api/dm/conversation/:id` | 🆕 |
| **メディア添付DM** | DM強化 | `POST /api/dm` に media_urls 追加 | 🆕 |
| **ブックマーク一覧** | Bookmark | `GET /api/bookmarks` | 🆕 |
| **ブックマーク追加/削除** | Bookmark | `POST/DELETE /api/bookmarks/:id` | 🆕 |
| **ユーザーのいいね一覧** | User | ✅ 既存（`GET /api/user/:id/likes`） |
| **フォロワー一覧** | User | ✅ 既存 |
| **フォロー中一覧** | User | ✅ 既存 |

### 4.3 GraphQLエンドポイント更新

`src/client.ts` の `ENDPOINTS` を最新の twikit ソースコードに基づいて更新する。以下のエンドポイントを追加：

```typescript
const ENDPOINTS = {
  // 既存 (更新)
  CreateTweet:      `${TWITTER_BASE}/i/api/graphql/.../CreateTweet`,
  DeleteTweet:      `${TWITTER_BASE}/i/api/graphql/.../DeleteTweet`,
  // ... 以下すべて最新のクエリIDに更新

  // 新規追加
  BookmarkTimeline:       `${TWITTER_BASE}/i/api/graphql/.../BookmarkTimeline`,
  BookmarksAllDelete:     `${TWITTER_BASE}/i/api/graphql/.../BookmarksAllDelete`,
  CreateBookmark:         `${TWITTER_BASE}/i/api/graphql/.../CreateBookmark`,
  DeleteBookmark:         `${TWITTER_BASE}/i/api/graphql/.../DeleteBookmark`,
  ListBySlug:             `${TWITTER_BASE}/i/api/graphql/.../ListBySlug`,
  ListCreation:           `${TWITTER_BASE}/i/api/graphql/.../ListCreation`,
  ListDeletion:           `${TWITTER_BASE}/i/api/graphql/.../ListDeletion`,
  ListLatestTweetsTimeline: `${TWITTER_BASE}/i/api/graphql/.../ListLatestTweetsTimeline`,
  ListMembers:            `${TWITTER_BASE}/i/api/graphql/.../ListMembers`,
  ListMemberships:        `${TWITTER_BASE}/i/api/graphql/.../ListMemberships`,
  ListsManagementPageTimeline: `${TWITTER_BASE}/i/api/graphql/.../ListsManagementPageTimeline`,
  NotificationDetail:     `${TWITTER_BASE}/i/api/graphql/.../NotificationDetail`,
  NotificationsPage:      `${TWITTER_BASE}/i/api/graphql/.../NotificationsPage`,
  SpaceById:              `${TWITTER_BASE}/i/api/graphql/.../SpaceById`,
  SpaceSearch:            `${TWITTER_BASE}/i/api/graphql/.../SpaceSearch`,
  TweetResultByRestId:    `${TWITTER_BASE}/i/api/graphql/.../TweetResultByRestId`,
  Vote:                   `${TWITTER_BASE}/i/api/graphql/.../Vote`,
  /// etc.
};
```

### 4.4 APIレスポンス整形

- 全エンドポイントのレスポンス形式を統一
- エラーレスポンスに詳細なエラーコードを含める
- ページネーション対応エンドポイントに統一的なカーソル形式を適用

---

## 5. 品質向上

### 5.1 ユニットテスト（バックエンド）

- **フレームワーク**: Vitest（※ 推奨。Cloudflare Workers互換性が高い）
- **テスト対象**:
  - `src/client.ts` — TwitterClient の各メソッド（fetchをモック）
  - `src/handlers/` — 各ハンドラーのリクエスト/レスポンス検証
  - `src/storage/` — KV/D1 ストアのロジック
  - `src/middleware/ratelimit.ts` — レート制限ロジック
- **カバレッジ目標**: 主要ロジック 70%以上

### 5.2 型定義強化

- `src/types.ts` の全 `any` 型を具体的な型に置き換え
- Twitter API レスポンスの型を追加（`TweetResult`, `UserResult`, `NotificationResult` など）
- 新規機能の型定義を追加（`List`, `Poll`, `Bookmark`, `Space`, `Notification` など）

### 5.3 エラーハンドリング改善

- 全ハンドラーでエラーメッセージを日本語/英語に対応
- エラーコードの体系化（認証エラー、バリデーションエラー、APIエラー、レート制限など）
- Twitter API エラー時のリトライ機構の検討

### 5.4 ドキュメント更新

- `README.md` / `README.en.md` を v0.2.0 に対応
  - 新しいページ・機能の一覧追加
  - 新しいAPIエンドポイントの一覧追加
  - アーキテクチャ図の更新（3カラムレイアウト反映）
- `CHANGELOG.md` を新規作成（オプション）

---

## 6. スコープ外（今回含めない）

| 項目 | 理由 |
|------|------|
| 複数アカウント対応 | アーキテクチャの大幅変更が必要なため後回し |
| E2Eテスト / CI/CDパイプライン | 時間制約により今回は見送り |
| i18n（多言語対応） | 現状日本語＋英語で十分と判断 |
| PWA対応 | 優先度低 |
| ダーク/ライトテーマ自動検出 | 優先度中（必要なら追加検討） |
| ストリーミングAPI（WebSocket） | 高度な実装が必要なため |

---

## 7. 実装の優先順位（1週間計画）

最大の効果を出すため、以下の順序で実装する：

### フェーズ1（Day 1-2）：基盤整備 ⚙️

| # | タスク | 詳細 |
|---|--------|------|
| 1 | GraphQLクエリID更新 | 全エンドポイントを最新のtwikit準拠に更新 |
| 2 | 型定義強化 | `any` を具体的な型に置き換え、新機能の型追加 |
| 3 | 右カラム（Widgets）実装 | 検索バー + トレンド表示 |

### フェーズ2（Day 3-4）：コア機能拡充 ⚡

| # | タスク | 詳細 |
|---|--------|------|
| 4 | ツイートカード刷新 | X風デザイン、ホバーアニメーション、インタラクション改善 |
| 5 | 3カラムレイアウト実装 | CSS Grid / Flexbox で左・中央・右カラム構成 |
| 6 | ブックマーク機能 | バックエンドAPI + フロントエンドUI |
| 7 | 通知機能 | 通知一覧API + フロントエンドページ |

### フェーズ3（Day 5-6）：UI全面リデザイン 🎨

| # | タスク | 詳細 |
|---|--------|------|
| 8 | 新レイアウトにページ移植 | 全8ページを3カラム＋X風に再構築 |
| 9 | サイドバー刷新 | アイコン中心のナビに変更 |
| 10 | リスト機能 | バックエンドAPI + フロントエンドページ |

### フェーズ4（Day 7）：仕上げ 🧹

| # | タスク | 詳細 |
|---|--------|------|
| 11 | ユニットテスト | 各ハンドラーのVitestテスト |
| 12 | DM強化 | 会話一覧＋メディア添付対応 |
| 13 | 投票機能 | 投票付きツイート投稿・投票 |
| 14 | 最終調整 | README更新、エラーハンドリング最終調整、デプロイ確認 |

### ボーナス（時間が余れば）

| # | タスク | 優先度 |
|---|--------|--------|
| 15 | プロフィールページ | ⭐ |
| 16 | スペース機能 | 🔽 |
| 17 | センシティブ設定 | 🔽 |

---

## 8. 技術スタック（現状維持・変更点）

| レイヤー | 現状 | v0.2.0 |
|----------|------|--------|
| フロントエンド | バニラJS + CSS | バニラJS + CSS（CSS変数拡張） |
| バックエンド | Hono + TypeScript | 変更なし |
| ストレージ | Cloudflare KV + D1 | 変更なし |
| テスト | なし | Vitest 導入 |
| ホスティング | Cloudflare Pages + Functions | 変更なし |

### 開発環境

```bash
npm install -D vitest       # テストランナー
npm install                 # 依存関係更新
```

---

## 9. リスクと注意点

| リスク | 影響 | 対策 |
|--------|------|------|
| GraphQLクエリIDが公開情報と異なる | 機能不全 | twikitの最新コードを必ず参照、動作確認 |
| 1週間の期限内に収まらない | 不完全なリリース | コア機能（フェーズ1-2）を最優先 |
| XのUIが頻繁に変わる | デザインの陳腐化 | 大まかな構造を合わせればOK、細部は追わない |
| 大量の型追加でビルド時間増加 | 開発効率低下 | 段階的に追加、並行作業でカバー |
| クッキー有効期限切れ | 全機能停止 | エラーハンドリングで事前検出＋メッセージ表示 |

---

## 10. デプロイ計画

```bash
# 1. バージョン更新
# package.json: "version": "0.2.0"
# src/handlers/admin.ts: version '0.2.0'

# 2. ビルド確認
npm run deploy  # wrangler pages deploy

# 3. 動作確認
# - /api/health でバージョン確認
# - 各ページの表示確認
# - 新機能の動作確認
```

---

## 11. 決定済み事項まとめ

| 項目 | 決定内容 |
|------|----------|
| 次のバージョン | **0.2.0** |
| 更新の種類 | **複合**（新機能 + UI改善 + 品質向上） |
| 新機能 vs UI | **50:50** バランス |
| レイアウト | **3カラム**（左:ナビ / 中央:メイン / 右:トレンド+検索） |
| 既存ページ | **すべてX風にリデザイン** |
| UI再現度 | **ツイートカード・サイドバー・全体レイアウト・プロフィールページ** |
| twikit機能 | **全機能**（リスト/スペース/通知/投票/センシティブ/DM強化） |
| マルチアカウント | **含めない** |
| GraphQL更新 | **最新twikit準拠に更新** |
| テスト | **バックエンドユニットテスト（Vitest推奨）** |
| 期限 | **1週間以内** |
